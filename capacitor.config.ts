import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'calculatrice-scientifique',
  webDir: 'www'
};

export default config;
